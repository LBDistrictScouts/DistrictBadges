apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: district-badges

resources:
  - core/namespace.yaml
  - core/service-account.yaml
  - secrets/one-password-item.yaml
  - config/app-config-map.yaml
  - config/nginx-config-map.yaml
  - db/service.yaml
  - db/stateful-set.yaml
  - app/deployment.yaml
  - app/service.yaml
  - worker/deployment.yaml
  - tls/certificate.yaml

labels:
  - pairs:
      app.kubernetes.io/name: district-badges
      app.kubernetes.io/part-of: district-badges
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: true
    includeTemplates: true

images:
  - name: ghcr.io/lbdistrictscouts/districtbadges/backend
    newTag: latest

commonAnnotations:
  author: "District Badges"
  description: "CakePHP backend deployment"

configurations:
  - kustomize-config.yaml
