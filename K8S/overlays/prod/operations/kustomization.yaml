apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: district-badges-prod

namePrefix: prod-

resources:
  - ../../../operations

labels:
  - pairs:
      environment: production
    includeSelectors: true
    includeTemplates: true

patches:
  - path: migrate-job-patch.yaml
